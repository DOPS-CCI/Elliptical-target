<Window x:Class="CircleTargetExperiment.FinalStats"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CircleTargetExperiment"
        mc:Ignorable="d" FontSize="24"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Title="Final Stats" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>
    <StackPanel Orientation="Vertical">
        <TextBlock>
            <Run Text="Number of trials = "/>
            <Run Name="NTrials"/>
        </TextBlock>
        <TextBlock>
            <Run Text="Overall p-value = "/>
            <Run Name="PVal"/>
        </TextBlock>
        <TextBlock>
            <Run Text="Overall final score = "/>
            <Run Name="ScoreTot"/>
        </TextBlock>
        <TextBlock>
            <Run Text="Total time = "/>
            <Run Name="Time"/>
        </TextBlock>
        <Grid HorizontalAlignment="Right">
            <Button IsDefault="True" Width="100" Height="40" FontWeight="Bold" Margin="10" Click="Button_Click">OK</Button>
        </Grid>
    </StackPanel>
</Window>
